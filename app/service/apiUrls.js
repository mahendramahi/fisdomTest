import {SONGS_BASE_URL, BASE_URL} from '@env';

export const SONGS_LIST = `${SONGS_BASE_URL}search?term=Michael+jackson`;

export const LOGIN = `${BASE_URL}login`;

export const USER_LIST = `${BASE_URL}users?page=1`;
